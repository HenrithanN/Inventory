import { makeStyles } from '@material-ui/core/styles';
import Card from '@material-ui/core/Card'
const afsa = makeStyles({
    root: {
      maxWidth: 345,
    },
    media: {
      height: 140,
    },
  });
  
export default Card;

